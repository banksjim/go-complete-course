{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Current Lesson",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${file}",
            "console": "integratedTerminal",
            "cwd": "${fileDirname}",
            "output": "${workspaceFolder}/bin/${fileBasenameNoExtension}"
        }
    ]
}
